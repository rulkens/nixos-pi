# Build outputs
result
artifacts/

# user config with secrets (generated by build-image.sh / deploy.sh)
config.json

# deploy target — may contain a local IP; example is committed instead
deploy-config.json

# Home Assistant backups — contains credentials, not committed by default
backups/

