{ ... }:
{
  # Timezone for the system clock. Change this to your local timezone.
  # Run `timedatectl list-timezones` to see all available options.
  time.timeZone = "Europe/Amsterdam";

  # Default locale used for language, number formatting, date formatting,
  # and other locale-dependent behaviour system-wide.
  i18n.defaultLocale = "en_US.UTF-8";
}
