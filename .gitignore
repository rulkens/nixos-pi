# Secret/personal configuration (generated by build.sh)
local-config.nix

# Build outputs
result
artifacts/

# user config with secrets (generated by build.sh)
config.json

# Nix flake lock (optional — some prefer to commit this)
# flake.lock